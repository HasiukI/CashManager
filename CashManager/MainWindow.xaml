<Window x:Class="CashManager.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CashManager.ViewModel"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800"
        >
    <Window.Resources>
        <Storyboard x:Key="ShowCreateCategory">
            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="gridCreateCategory" Storyboard.TargetProperty="(UIElement.Visibility)">
                <DiscreteObjectKeyFrame KeyTime="00:00:00" Value="{x:Static Visibility.Visible}"/>
                <DiscreteObjectKeyFrame KeyTime="00:00:01" Value="{x:Static Visibility.Visible}"/>
            </ObjectAnimationUsingKeyFrames>
            <DoubleAnimationUsingKeyFrames Storyboard.TargetName="gridCreateCategory" Storyboard.TargetProperty="(FrameworkElement.Width)">
                <EasingDoubleKeyFrame KeyTime="00:00:00" Value="0"/>
                <EasingDoubleKeyFrame KeyTime="00:00:01" Value="300"/>
            </DoubleAnimationUsingKeyFrames>
            <BooleanAnimationUsingKeyFrames Storyboard.TargetName="grid" Storyboard.TargetProperty="(UIElement.IsEnabled)">
                <DiscreteBooleanKeyFrame KeyTime="00:00:00" Value="True"/>
                <DiscreteBooleanKeyFrame KeyTime="00:00:01" Value="False"/>
            </BooleanAnimationUsingKeyFrames>
        </Storyboard>
        <Storyboard x:Key="CloseCreateCategory">
            <DoubleAnimationUsingKeyFrames Storyboard.TargetName="gridCreateCategory" Storyboard.TargetProperty="(FrameworkElement.Width)">
                <EasingDoubleKeyFrame KeyTime="00:00:00" Value="295"/>
                <EasingDoubleKeyFrame KeyTime="00:00:01" Value="0"/>
                <EasingDoubleKeyFrame KeyTime="00:00:01.1000000" Value="0"/>
            </DoubleAnimationUsingKeyFrames>
            <BooleanAnimationUsingKeyFrames Storyboard.TargetName="grid" Storyboard.TargetProperty="(UIElement.IsEnabled)">
                <DiscreteBooleanKeyFrame KeyTime="00:00:00" Value="False"/>
                <DiscreteBooleanKeyFrame KeyTime="00:00:00.7000000" Value="True"/>
            </BooleanAnimationUsingKeyFrames>
        </Storyboard>
    </Window.Resources>

    <Window.DataContext>
        <local:MainViewModel/>
    </Window.DataContext>

    <!--<Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="400*"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="25*"/>
            <RowDefinition Height="48*"/>
            <RowDefinition Height="362*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="1" Margin="0,47,0,0" Grid.RowSpan="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition/>
                </Grid.RowDefinitions>
                <Grid>
                    <StackPanel>
                        <TextBlock Text="{Binding CurentCategory.Name, Mode=OneWay}"/>
                        <TextBlock Text="{Binding CurentCategory.Price, Mode=OneWay}"/>
                    </StackPanel>
                </Grid>
                -->
    <!--Category-->
    <!--
                <Grid Grid.Row="2">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>

                    
                    
                    <ListBox ItemsSource="{Binding Categories}" SelectedItem="{Binding CurentCategory}">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Button Command="">
                                    <TextBlock Text="{Binding Name}"/>
                                </Button>
                               
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>

                    <Grid x:Name="grid" Grid.Column="1">


                        <Button x:Name="button" Content="+Category">
                            
                        </Button>


                        <StackPanel  x:Name="stackPanel"  Grid.Column="1">
                            <Label>name</Label>
                            <TextBox Text="{Binding NameCategory, Mode=TwoWay}"></TextBox>
                            <Label>name</Label>
                            <TextBox Text="{Binding PriceCategory,Mode=TwoWay}"></TextBox>
                            <Label>isCosts</Label>
                            <CheckBox IsChecked="{Binding IsCosts, Mode=TwoWay}"></CheckBox>
                            <Button Command="{Binding CreateNewCategory}">add</Button>
                            <Button Content="Close">
                                -->
    <!--<Button.Triggers>
                                    <EventTrigger RoutedEvent="Button.Click">
                                        <BeginStoryboard Storyboard="{StaticResource CloseCategoryCreate}" />
                                    </EventTrigger>
                                </Button.Triggers>-->
    <!--
                            </Button>
                        </StackPanel>
                    </Grid>
                    

                </Grid>
            </Grid>
        </Grid>

        <Popup Placement="Right" Width="100" Height="100" PopupAnimation="Slide" AllowsTransparency="True" IsOpen="True">
            <StackPanel  x:Name="stackPanel1"  Grid.Column="1" Background="Blue">
                <Label>name</Label>
                <TextBox Text="{Binding NameCategory, Mode=TwoWay}"></TextBox>
                <Label>name</Label>
                <TextBox Text="{Binding PriceCategory,Mode=TwoWay}"></TextBox>
                <Label>isCosts</Label>
                <CheckBox IsChecked="{Binding IsCosts, Mode=TwoWay}"></CheckBox>
                <Button Command="{Binding CreateNewCategory}">add</Button>
                <Button Content="Close">
                   <Button.Triggers>
              <EventTrigger RoutedEvent="Button.Click">
                  <BeginStoryboard Storyboard="{StaticResource CloseCategoryCreate}" />
              </EventTrigger>
          </Button.Triggers>
                </Button>
            </StackPanel>
        </Popup>
    </Grid>-->


    <!--<Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        
        <Grid Background="BlueViolet">
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            
            <StackPanel  x:Name="stackPanel1"  Grid.Column="1" >
                <Label>name</Label>
                <TextBox Text="{Binding Category.NameCategory, Mode=TwoWay}"></TextBox>
                <Label>name</Label>
                <TextBox Text="{Binding Category.PriceCategory,Mode=TwoWay}"></TextBox>
                <Label>isCosts</Label>
                <CheckBox IsChecked="{Binding Category.IsCostsCategory, Mode=TwoWay}"></CheckBox>
                <Button Command="{Binding Category.CreateNewCategory}">add</Button>
            </StackPanel>

            <ListBox Grid.Row="1" ItemsSource="{Binding Category.Categories}" SelectedItem="{Binding Cash.CurentCategory}">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Button Command="">
                            <TextBlock Text="{Binding Name}"/>
                        </Button>

                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
            
        </Grid>

        <Grid Grid.Column="1" Background="Blue">
            <StackPanel>
                <Label>Name</Label>
                <TextBlock Text="{Binding Cash.CurentCategory.Name, Mode=OneWay}"/>
                <Label>Price</Label>
                <TextBox Text="{Binding Cash.CurentCategory.Price, Mode=OneWay}"/>
                <Label>Count</Label>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="{Binding Cash.CountCash}"></TextBlock>
                    <Button Command="{Binding Cash.CountUpCash}">+</Button>
                    <Button Command="{Binding Cash.CountDownCash}">-</Button>
                </StackPanel>
                <Label>Total</Label>
                <TextBlock Text="{Binding Cash.TotalCash}"></TextBlock>
                <Button Content="SetPrice" Command="{Binding Cash.CreateCashItem}"/>
            </StackPanel>
        </Grid>
    </Grid>-->
    <Grid>
        <Grid x:Name="grid">
            <StackPanel>
                <Button Content="Create Category">
                    <Button.Triggers>
                        <EventTrigger RoutedEvent="Button.Click">
                            <BeginStoryboard Storyboard="{StaticResource ShowCreateCategory}" />
                        </EventTrigger>
                    </Button.Triggers>
                </Button>

                <ListBox ItemsSource="{Binding Category.Categories}">
                    <ListBox.ItemsPanel>
                        <ItemsPanelTemplate>
                            <UniformGrid Columns="3"/>
                        </ItemsPanelTemplate>
                    </ListBox.ItemsPanel>
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel>
                                <Border Width="50" Height="50" Background="{Binding Color}" CornerRadius="50">
                                    <Image Source="{Binding Image}" Width="40"></Image>
                                </Border>
                                <TextBlock Text="{Binding Name}"></TextBlock>
                            </StackPanel>
                           
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </StackPanel>
        </Grid>

        <Grid x:Name="gridCreateCategory" Background="#FF001AFF" Width="0" HorizontalAlignment="Right" Visibility="Hidden">
            <StackPanel  x:Name="stackPanel1"  Grid.Column="1" >
                <Button Content="Close">
                    <Button.Triggers>
                        <EventTrigger RoutedEvent="Button.Click">
                            <BeginStoryboard Storyboard="{StaticResource CloseCreateCategory}" />
                        </EventTrigger>
                    </Button.Triggers>
                </Button>
                <Label>name</Label>
                <TextBox Text="{Binding Category.NameCategory, Mode=TwoWay}"></TextBox>
                <Label>name</Label>
                <TextBox Text="{Binding Category.PriceCategory,Mode=TwoWay}"></TextBox>
                <Label>isCosts</Label>
                <CheckBox IsChecked="{Binding Category.IsCostsCategory, Mode=TwoWay}"></CheckBox>
                
                <ListBox ItemsSource="{Binding Category.Colors}" SelectedItem="{Binding Category.ColorCategory}">
                    <ListBox.ItemsPanel>
                        <ItemsPanelTemplate>
                            <UniformGrid Columns="3"/>
                        </ItemsPanelTemplate>
                    </ListBox.ItemsPanel>
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Button Width="36" Padding="5" >
                                <Ellipse  Width="25" Height="25" Fill="{Binding}"/>
                            </Button>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>

                <ListBox ItemsSource="{Binding Category.ImagesName}"  SelectedItem="{Binding Category.ImageCategory}">
                    <ListBox.ItemsPanel>
                        <ItemsPanelTemplate>
                            <UniformGrid Columns="3"/>
                        </ItemsPanelTemplate>
                    </ListBox.ItemsPanel>
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Button>
                                <Image Source="{Binding}" Width="25"></Image>
                            </Button>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
       
                
                <Button Command="{Binding Category.CreateNewCategory}">add</Button>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
